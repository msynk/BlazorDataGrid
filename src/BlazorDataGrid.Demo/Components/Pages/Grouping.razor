@page "/grouping"

<PageTitle>Grouping &amp; Aggregates</PageTitle>

<h1>Grouping &amp; aggregates</h1>
<p class="lead">Group rows by a column and compute per-group and grand-total aggregates.</p>

<p>
    Click the <strong>⊞</strong> button in a groupable column header to group by it (click again to
    ungroup). Each group is collapsible and shows its own aggregates. The footer shows grand totals.
</p>

<div class="card">
    <BlazorDataGrid TItem="Product" Items="_products" Height="500px"
              Groupable="true" ShowFooter="true" Sortable="true">
        <BlazorDataGridColumn TItem="Product" Field="Id" Title="ID" Width="70px" Align="BlazorDataGridColumnAlign.Right" Groupable="false" />
        <BlazorDataGridColumn TItem="Product" Field="Name" Width="200px" Groupable="false" />
        <BlazorDataGridColumn TItem="Product" Field="Category" />
        <BlazorDataGridColumn TItem="Product" Field="Supplier" />
        <BlazorDataGridColumn TItem="Product" Field="Price" Format="C2" Align="BlazorDataGridColumnAlign.Right"
                        Aggregate="BlazorDataGridAggregateType.Sum" Groupable="false" />
        <BlazorDataGridColumn TItem="Product" Field="Stock" Align="BlazorDataGridColumnAlign.Right"
                        Aggregate="BlazorDataGridAggregateType.Average" AggregateFormat="N0" Groupable="false" />
        <BlazorDataGridColumn TItem="Product" Field="Rating" Format="N1" Align="BlazorDataGridColumnAlign.Right"
                        Aggregate="BlazorDataGridAggregateType.Max" Groupable="false" />
    </BlazorDataGrid>
</div>

<p class="hint">Try grouping by <strong>Category</strong> or <strong>Supplier</strong>. Group by both to see
multi-level (nested) grouping — each level is independently collapsible.</p>

<h2>Markup</h2>
<div class="card">
<pre><code>&lt;BlazorDataGrid TItem="Product" Items="products"
                Groupable="true" ShowFooter="true" Sortable="true"&gt;
    &lt;BlazorDataGridColumn TItem="Product" Field="Name" Groupable="false" /&gt;
    &lt;BlazorDataGridColumn TItem="Product" Field="Category" /&gt;
    &lt;BlazorDataGridColumn TItem="Product" Field="Price" Format="C2"
                          Aggregate="BlazorDataGridAggregateType.Sum" Groupable="false" /&gt;
    &lt;BlazorDataGridColumn TItem="Product" Field="Stock"
                          Aggregate="BlazorDataGridAggregateType.Average" AggregateFormat="N0" Groupable="false" /&gt;
&lt;/BlazorDataGrid&gt;</code></pre>
</div>

@code {
    private List<Product> _products = SampleData.Generate(80);
}
